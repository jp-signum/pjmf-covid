import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PJMF Covid 19 Tracker",
  description:
    "A full-stack web application that visualizes monthly COVID-19 hospitalization rates using CDC COVID-NET data. Built with Next.js, TypeScript, and Chart.js, the app fetches and processes live public health data, displays national hospitalization trends over time, and enables users to explore state-level insights through interactive charts and searchable tables.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
